rc: ./.lefthookrc
pre-commit:
  parallel: true
  commands:
    check-and-lint:
      glob: "**/*.{ts,tsx}"
      run: bun biome check --write {staged_files}
      stage_fixed: true

commit-msg:
  only:
    - ref: 'refs/heads/main'
  commands:
    commitlint:
      run: bun commitlint --edit {1}