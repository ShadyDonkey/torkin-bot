import './schemas/better-auth'
import './schemas/user'

// This is defined in better-auth
// datasource db {
//     provider = 'postgresql'
//     url = env('DATABASE_URL')
// }

model DiscordWebhookEventLog {
  id        String   @id @default(cuid(2)) @map('id')
  type      String   @map('type')
  data      Json     @map('data')
  timestamp DateTime @default(now()) @map('timestamp')

  @@map('discord_webhook_event_log')
}
